{
  "manifest_version": 3,
  "name": "Salesforce Hours Approver",
  "version": "1.0",
  "description": "One-click approval of Salesforce timecards — rejects overhead, approves the rest.",
  "permissions": ["activeTab", "scripting", "tabs"],
  "host_permissions": ["https://*.lightning.force.com/*"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "Approve Hours"
  }
}
